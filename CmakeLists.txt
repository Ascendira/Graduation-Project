cmake_minimum_required(VERSION 3.10)
project(MNS_Precipitation_Model)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

list(APPEND CMAKE_PREFIX_PATH "D:/Language/MSYS2/ucrt64")
find_package(SUNDIALS REQUIRED COMPONENTS 
    cvode 
    nvecserial 
    sunmatrixband 
    sunlinsolband
)

set(PROJECT_SOURCES
    MNS_CD.cpp 
    Input.cpp
)

add_executable(MNS_Model ${PROJECT_SOURCES})

target_link_libraries(MNS_Model 
    PRIVATE
    SUNDIALS::cvode_static 
    SUNDIALS::nvecserial_static
    SUNDIALS::sunlinsolband_static
    SUNDIALS::core_static
    m
)

target_include_directories(MNS_Model PRIVATE ${PROJECT_SOURCE_DIR})